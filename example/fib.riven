
program {
    fn fib(n) {
      if (n = 0) {
        return 0;
      }

      if (n = 1) {
        return 1;
      }

      fib_n = fib(n-1) + fib(n-2);
      printf("fib(%d): %d\n", n, fib_n);
      return fib_n;
    }

    fn main() {
      fib((1+1) * 2);
      return 0;
    }
  }


